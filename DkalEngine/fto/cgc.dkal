#include "common.dkal"
#set private_sql "Server=tcp:wmmk2vmk5y.database.windows.net;Database=ftoCgc;User ID=fto;Password=yaeche9H;Trusted_Connection=False;Encrypt=True;"
#set me "_cgc"

// _cgc's knowledge:
_cgc knows 
  if
    asInfon( C == croOf(T) )
  then
    C tdonI X can read V in records of trial T under the authority of P

#set initial_input "X can read R in records of trial T under the authority of P"

